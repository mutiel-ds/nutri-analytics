"""Páginas provisionales, pendientes de implementación en subpasos posteriores del plan."""

import streamlit as st


def planificador() -> None:
    """Stub de la página Planificador (subpaso 3.2 del plan)."""
    st.title("📅 Planificador")
    st.info("En construcción (subpaso 3.2/3.3/3.4 del plan).")


def recetario() -> None:
    """Stub de la página Recetario (subpaso 3.2/3.3 del plan)."""
    st.title("🍳 Recetario")
    st.info("En construcción (subpaso 3.2/3.3/3.4 del plan).")


def lista_compra() -> None:
    """Stub de la página Lista de la compra (subpaso 3.3/3.4 del plan)."""
    st.title("🛒 Lista de la compra")
    st.info("En construcción (subpaso 3.2/3.3/3.4 del plan).")


def metricas() -> None:
    """Stub de la página Métricas (subpaso 3.4 del plan)."""
    st.title("📈 Métricas")
    st.info("En construcción (subpaso 3.2/3.3/3.4 del plan).")
